package com.scripts.sound {    import flash.display.Sprite;    import flash.events.*;    import flash.media.Sound;    import flash.media.SoundChannel;    import flash.net.URLRequest;    public class soundLoader extends Sprite {        		public var urlSong:String;        public var song:Sound;		public var soundChannel:SoundChannel;		public var songPosition:Number = 0;        public function soundLoader(s:String) 		{            urlSong = s;			getSound();        }		private function getSound()		{						var request:URLRequest = new URLRequest(urlSong);            song = new Sound();            song.addEventListener(Event.COMPLETE, completeHandler);            song.addEventListener(IOErrorEvent.IO_ERROR, ioErrorHandler);           	song.addEventListener(ProgressEvent.PROGRESS, progressHandler);            song.load(request);		}        private function completeHandler(event:Event):void {            //trace("completeHandler: " + event);			dispatchEvent(new Event(Event.COMPLETE));        }		private function ioErrorHandler(event:Event):void {            //trace("ioErrorHandler: " + event);        }        private function progressHandler(event:ProgressEvent):void {           // trace("progressHandler: " + event);        }    }}